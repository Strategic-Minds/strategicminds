# Validation Gate

No production release until homepage loads, all routes render, buttons route correctly, light/dark mode works, PWA installability works, signup/login works, client dashboard is protected, admin dashboard is protected, Supabase RLS is verified, Stripe test checkout passes, webhook test passes, cron secret is required, 5-minute cron logs safe checks, AI Gateway refuses unsafe requests, AI call feature has approval gate, release receipt is generated, and rollback plan exists.
